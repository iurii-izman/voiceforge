# Промпт для реализации плана развития (новый чат)

Скопируй блок ниже в начало нового чата.

---

```
Проект VoiceForge. Контекст: @docs/runbooks/agent-context.md (правила, конфиг, приоритеты). Работай по нему без поиска по проекту. Приоритет фич — docs/roadmap-priority.md. Эффективно и дёшево; ключи в keyring; Fedora Atomic/toolbox/uv.

Реализовать план развития по аудиту февраля 2026: @docs/development-plan-post-audit-2026.md

Порядок:
1. Сверь пункты Части I и II с текущим кодом — что уже сделано (например export, --template, Ollama в конфиге, action-items update), не дублируй.
2. Часть I — приоритетные задачи 1→10: делать по одной, начиная с первых нереализованных (--template, streaming в listen, history --format md, status --detailed, history --search, и т.д.).
3. Часть II — слабые места W1–W10: сначала W1 (бюджет из Settings), W2 (sample_rate/ресэмплинг), W3 (RAG длина), W8 (валидация Settings); остальные по возможности.
4. После каждой фичи: тесты при необходимости, обновление docs/runbooks/config-env-contract.md если менялся контракт, CHANGELOG для user-facing изменений. Не нарушать ADR-0001 (новые команды только через ADR; флаги к существующим командам — можно).
```

---

Конец промпта.
