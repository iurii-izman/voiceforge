# ADR-0001: Core Scope for alpha0.1

Status: Accepted

Scope is strict minimal runnable core with 9 CLI commands:
`listen`, `analyze`, `status`, `history`, `index`, `watch`, `daemon`, `install-service`, `uninstall-service`.
